/* Bounds the client enforces and the server re-checks. Shared so the form cannot offer what the route will reject. */

/** Rivers accepted per run. */
export const MAX_RIVERS = 25;

/** Characters kept per river name. */
export const MAX_RIVER_CHARS = 60;
